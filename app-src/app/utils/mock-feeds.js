const USE_MOCK_DATA = true;
const VZ_SD = '/starter/';

if( USE_MOCK_DATA ){

    const MOCK_FEEDS = {
        APP: VZ_SD + 'mock/json/app-home.json'
    }

    //for( let feed in MOCK_FEEDS ){
       // FEEDS[ feed ] = MOCK_FEEDS[ feed ];
    //}

}
